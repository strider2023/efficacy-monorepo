export enum UITableColumnTypes {
    STRING = 'string',
    NUMBER = 'number',
    DATE = 'date',
    DATETIME = 'dateTime',
    BOOLEAN = 'boolean',
    SINGLE_SELECT = 'singleSelect',
    ACTIONS = 'actions',
}